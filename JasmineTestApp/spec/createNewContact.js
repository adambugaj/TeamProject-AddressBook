  describe('test addressbook', function() {
    "use strict";

    var getFirstLast = "Adam Bugaj";
    var getFirstLast1 = getFirstLast1;

  it('should be able to create a new contact', function() {
    expect(contactList).toBe(newContact);
  })
  it('should be able to create a second new contact', function() {
  expect(contactList).toBe(newContact1);
});
  it('should be able to show first name and last name', function() {
    expect(getFirstLast).toBe(getFirstLast1);
});
  });
